# BetterCensorship

This repository contains the static assets for the BetterCensorship demo.

## Running Tests

The project includes a simple Node.js test under `tests/`.
Run it with:

```bash
node tests/pngChunks.test.js
```
